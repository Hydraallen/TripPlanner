# TripPlanner Configuration
# Copy this file to .env and fill in your values.

# === POI Data ===
# International: Overpass API + Nominatim (free, no key needed)
# Chinese destinations: Amap (requires key below)

# Amap API key (for Chinese destinations)
# Get yours at https://lbs.amap.com/ (free, 5k calls/day)
AMAP_API_KEY=

# === Geoapify (optional) ===
# Used for reverse geocoding (fetching addresses for POIs)
# Get yours at https://www.geoapify.com/ (free tier available)
GEOAPIFY_API_KEY=

# === LLM (GLM, optional) ===
# Used for AI-powered plan generation and chat
# Get yours at https://open.bigmodel.cn/
OPENAI_ENDPOINT=https://open.bigmodel.cn/api/coding/paas/v4
OPENAI_API_KEY=
OPENAI_MODEL_NAME=glm-5.1
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=16384

# === Database ===
# Default: SQLite (no setup needed)
# Docker: postgresql+asyncpg://tripplanner:tripplanner@postgres:5432/tripplanner
DATABASE_URL=sqlite+aiosqlite:///./trips.db

# === Server ===
HOST=0.0.0.0
PORT=8000
CORS_ORIGINS=*
